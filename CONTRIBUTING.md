# Scripting 仓库编辑与维护说明

> 本文面向仓库维护者和脚本作者。新增、修改或整理 Scripting 项目时，请按本文执行。

## 一、仓库定位

这是一个用于收录 Scripting App 脚本的长期项目库。仓库中的每个脚本都是一个可以单独导入、单独运行和单独维护的完整项目。

仓库不存放零散代码，也不保留 `examples/` 等教学样例。正式脚本统一放在 `Scripting/` 目录下。

## 二、目录结构

每个脚本直接放在 `Scripting/` 下，不要再创建 `Scripts/` 中间目录：

```text
Scripting/我的脚本/
├── script.json          # 项目配置与版本号
├── index.tsx            # 主运行入口
├── README.md            # 使用说明与更新日志
├── widget.tsx           # 有小组件时保留
├── intent.tsx           # 有快捷指令或分享入口时保留
└── 其他文件              # 页面、组件、服务、资源等
```

### 目录要求

- 一个脚本对应一个独立目录。
- 脚本目录必须位于 `Scripting/脚本名称/`。
- 入口文件、组件、资源和配置必须一并提交，不能只提交主文件。
- 目录名称应简洁、稳定、容易识别，避免使用临时日期或无意义后缀。
- 不要提交调试文件、构建产物、本地路径或临时备份。
- 不要在仓库中创建 `examples/` 或其他教学目录。

## 三、新增脚本流程

### 1. 创建项目目录

在 `Scripting/` 下新建项目目录，并准备完整的 Scripting 项目文件：

```text
Scripting/天气查询/
```

### 2. 检查项目配置

确认 `script.json` 中包含正确的项目名称、入口配置和版本号。新增项目默认从 `1.0.0` 开始。

### 3. 编写项目说明

每个项目都必须有自己的 `README.md`，至少包含以下内容：

| 内容 | 应该写什么 |
| --- | --- |
| 项目简介 | 这个脚本解决什么问题，主要功能是什么 |
| 首次使用 | 导入后如何启动、登录、配置和完成第一次操作 |
| 配置权限 | API Key、Token、Cookie、账号、定位、相册等要求 |
| 使用说明 | 主要页面、按钮和常见操作的作用 |
| 注意事项 | 失效条件、数据删除风险、第三方服务和内容授权 |
| 更新日志 | 按版本记录每次功能变化，最新版本放在最上面 |

说明必须根据实际代码和页面编写。不要使用“运行即可使用”“功能丰富”“自行探索”等没有实际帮助的描述。

### 4. 更新仓库首页

在根目录 `README.md` 的脚本清单中新增：

- 脚本名称；
- 简洁但准确的功能描述；
- 指向该脚本目录的单独导入链接。

## 四、单项目导入链接

Scripting 不支持一次导入整个仓库。每个脚本必须使用独立导入链接，链接目标是脚本目录：

```markdown
[我的脚本](https://scripting.fun/import_scripts?urls=%5B%22https%3A%2F%2Fgithub.com%2F7452323%2FScripting%2Ftree%2Fmain%2FScripting%2F我的脚本%22%5D)
```

目录名称包含中文、空格或特殊符号时，必须进行网址编码。导入链接中的路径必须使用当前仓库地址和当前目录结构。

## 五、版本号与更新日志

脚本功能发生变化时，必须同时修改：

1. `script.json` 中的 `version`。
2. 项目 `README.md` 中的更新日志。

版本号使用三段式格式：

| 版本类型 | 示例 | 适用情况 |
| --- | --- | --- |
| 修订版本 | `1.0.1` | 修复问题、兼容性调整、小幅体验改进 |
| 次版本 | `1.1.0` | 新增向后兼容的功能、页面或配置项 |
| 主版本 | `2.0.0` | 修改配置格式、删除功能或其他不兼容变更 |

更新日志示例：

```markdown
## 更新日志

### 1.1.0 · 2026-08-23
- 新增按关键词筛选。
- 增加刷新按钮和加载状态提示。

### 1.0.1 · 2026-08-20
- 修复网络请求失败时页面卡住的问题。

### 1.0.0 · 2026-08-18
- 首次发布。
```

仅修改文字说明、没有改变脚本功能时，可以不递增 `script.json` 版本号，但不得删除已有更新记录。

## 六、权限与敏感配置

项目说明中必须明确列出所需权限和配置。特别是以下情况：

- 使用联系人、相册、定位、通知或文件权限；
- 需要填写 API Key、Token、Cookie 或登录账号；
- 会上传文件、保存媒体或删除本机数据；
- 依赖第三方网站、接口、代理或订阅服务；
- 展示或处理可能受版权、隐私或平台规则限制的内容。

仓库中严禁提交真实 Token、Cookie、账号密码、私钥或其他凭据。示例配置必须使用占位符，例如 `YOUR_API_KEY`。

## 七、提交前检查

提交或合并前，请逐项确认：

- [ ] 项目位于 `Scripting/项目名称/`。
- [ ] 项目可以独立导入，不依赖仓库中的其他项目。
- [ ] `script.json`、入口文件、组件和资源完整。
- [ ] 项目 `README.md` 已写明用途、首次使用、配置权限、注意事项和更新日志。
- [ ] 功能更新时，版本号和更新日志已经同步修改。
- [ ] 根目录 `README.md` 已添加准确描述和单独导入链接。
- [ ] 代码中没有真实 Token、Cookie、账号密码、私钥或本地路径。
- [ ] 没有提交调试输出、临时文件、构建产物或教学样例。
- [ ] 已检查中文目录、空格和特殊字符对应的导入链接是否正确。

## 八、提交信息建议

提交信息应直接说明改动内容，建议使用以下格式：

```text
新增天气查询项目
修复联系人管理器重复联系人问题
更新 Surge Pro 网络状态页面
完善每日图文项目使用说明
```

## 九、维护原则

- 先保证项目能用，再补充视觉和文档细节。
- 功能描述必须来自实际代码，不夸大、不含糊。
- 每次功能更新都留下清晰的版本记录。
- 尽量保持已有项目目录稳定，避免无必要的路径变更。
- 发现失效接口、过期配置或误导说明时，应及时修正并记录。

---

[返回脚本库首页](README.md) · [浏览全部项目](https://github.com/7452323/Scripting/tree/main/Scripting)