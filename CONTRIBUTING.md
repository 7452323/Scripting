# 收录和维护脚本

这个仓库是 Scripting 脚本的长期归档库。所有正式项目直接放在 `Scripting/项目名称/` 下，每个目录只维护一个可独立导入的项目。

## 项目结构

使用下面的项目结构：

```text
Scripting/我的脚本/
├── script.json
├── index.tsx
├── README.md
└── 其他项目文件/
```

其中：

- `script.json`：Scripting 项目配置，必须包含当前版本号。
- `index.tsx`：普通脚本的运行入口；如果项目使用 Widget、Intent、Live Activity 等其他入口，也请保留对应文件。
- `README.md`：脚本的首次使用说明和版本更新日志，必须随项目维护。
- 其他文件：组件、页面、服务、存储、Widget、Intent 和资源文件。

不要创建 `examples/` 或其他只用于教学的目录。仓库只收录可独立维护和导入的正式脚本项目。

## README 质量要求

每个脚本目录必须有自己的 `README.md`，至少包含：

- 用途：说明脚本实际解决的问题和主要功能。
- 首次使用：从导入、首次配置到完成主要操作的具体步骤。
- 注意事项：需要的权限、API Key、Token、Cookie、外部服务、删除风险或内容授权限制。
- 版本更新：按版本倒序记录变更，新增项目从 `1.0.0` 开始。

不要使用没有信息量的说明，例如“运行即可使用”。涉及账号、Cookie、Token、相册、联系人、定位、通知或文件删除时，必须明确提示。

## 版本与更新日志

每次修改脚本功能时，必须同时更新两处：

1. `script.json` 中的 `version`。
2. `README.md` 中的“版本更新”条目。

版本采用 `主版本.次版本.修订版本`：

- `1.0.1`：修复 bug、小型兼容性或体验改进。
- `1.1.0`：新增向后兼容的功能、入口或配置项。
- `2.0.0`：删除功能、修改配置格式或其他不兼容变更。

README 的最新版本条目放在最前面，并说明用户能感知到的变化。只修正文档且没有改动脚本功能时，不必修改 `script.json` 版本号，但不得删改已有的版本历史。

## 导入链接规则

Scripting 不支持把仓库目录内的所有脚本一次性导入。每个脚本都必须有自己的导入链接，链接指向该脚本目录：

```md
[我的脚本](https://scripting.fun/import_scripts?urls=%5B%22https%3A%2F%2Fgithub.com%2F7452323%2FScripting%2Ftree%2Fmain%2FScripting%2FScripts%2F我的脚本%22%5D)
```

仓库首页的脚本清单使用“脚本名 + 描述”的形式，脚本名本身就是导入链接。目录名包含中文或空格时，必须进行 URL 编码。

脚本自己的 README 可以提供同样的单独导入链接，并附上安装入口：

```md
[安装 Scripting App](https://apps.apple.com/app/apple-store/id6479691128)
```

## 提交前检查

- 确认脚本目录可以独立导入，不要把整个 `Scripting/Scripts` 目录作为导入对象。
- 确认入口文件和依赖文件都已提交，不要只提交 `index.tsx`。
- 确认 `README.md` 已包含准确的首次使用说明、权限/配置要求和版本更新记录。
- 功能有变更时，确认 `script.json` 版本与 README 更新日志同步更新。
- 在根目录 README 的“脚本清单”中添加或修正脚本名称、简短描述和单独导入链接。
- 删除调试输出、临时文件和本地构建产物。
- 检查代码中没有 Token、Cookie、账号密码、私钥或个人路径。
- 如果依赖外部 API、订阅地址或特殊权限，在脚本 README 中写明。
- 如果使用第三方代码或资源，注明来源和许可证。

## 目录和命名

- 脚本放在 `Scripting/脚本名称/`。
- 一个脚本一个目录，不要把多个项目混在一起。
- 目录名保持清晰稳定，避免无意义的日期或临时后缀。
- 修改已有脚本时，尽量保留原来的目录结构和入口文件。

## 提交信息

提交信息直接说明变更内容，例如：

```text
新增天气查询脚本
修复联系人管理器存储问题
更新 Surge Pro 地图资源
```