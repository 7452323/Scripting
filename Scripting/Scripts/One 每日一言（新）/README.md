# One 每日一言（新）

## 用途
从 ONE 每日接口获取当天文字、作者和图片，缓存图片并展示内容，支持小号、中号和大号 Widget 预览。

## 首次使用
1. 导入并运行脚本。
2. 运行脚本获取当天内容，首次使用时允许网络和文件缓存操作。
3. 按需预览不同尺寸的 Widget。
4. 将对应 Widget 添加到主屏幕，并在需要时从脚本刷新内容。

## 注意事项
内容来自第三方接口，接口不可用时可能只能显示缓存。图片和文字请遵守原服务的使用规则。

## 版本更新
### 1.0.0
- 首次收录。
- 支持每日内容获取、图片缓存和三种 Widget 预览。
