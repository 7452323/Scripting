# 负一屏低栏

## 用途
配置中号低栏 Widget 的外观，可启用透明背景，并分别设置天气栏和每日一句/节气内容区的背景颜色。

## 首次使用
1. 导入并运行脚本。
2. 打开透明背景开关，按需选择天气栏和内容区颜色。
3. 保存设置并预览 Widget。
4. 将对应的中号低栏 Widget 添加到主屏幕或负一屏。

## 注意事项
透明效果依赖 Widget 背景和系统环境。修改设置后，系统 Widget 可能需要手动刷新才能显示最新颜色。

## 版本更新
### 1.0.0
- 首次收录。
- 支持透明背景、天气栏颜色、每日一句/节气区域颜色和 Widget 预览。
